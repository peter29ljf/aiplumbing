The property type is on the ticket. Read it and pick the way out. Nothing else happens here
and you say nothing to the customer about it.

| what was recorded | the way out |
|---|---|
| `house`, `townhouse`, `low_rise`, `commercial` | `ok` |
| `building_project` | `building_project` |
| `apartment_home` | `declined` |

**`commercial` is `ok`.** Our insurance excludes strata dwellings, not tall buildings —
an office on the twelfth floor is ordinary work and turning it away is turning away work
we can do.

If nothing was recorded, treat it as `ok` and let the later steps ask. Guessing `declined`
turns somebody away on no evidence, which is the one mistake here that cannot be undone.
